import AdminLogin from "../login/admin-log";

export const metadata = {
  title: "Admin & Safety Portal | ForeSite",
  description: "Sign in to ForeSite administrative portal",
};

export default function AdminLoginPage() {
  return <AdminLogin />;
}
